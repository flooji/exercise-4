# comunica-sparql https://solid.interactions.ics.unisg.ch/florence/profile/card#me -f src/queries/persons.sparql
PREFIX  foaf: <http://xmlns.com/foaf/0.1/>

SELECT ?person
WHERE {
    ?me foaf:knows ?person.
}
